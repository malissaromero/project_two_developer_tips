<%= form_for(@post) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
<!-- mms: this can be tough - in concept and practice -->
  <%= collection_check_boxes(:post, :category_ids, Category.all, :id, :name) %>

  <%= f.label :command %>
  <%= f.text_field :command %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>

<% end %>
