<h1> <%= link_to "Back to Home", posts_path %> </h1>

<% @user.posts.each do |post| %>
  <div>
    <p> <%= link_to "#{post.title}", post_path(post) %></p>
    <p> <%= post.command %> </p>
    <p> <%= post.description %> </p>
  </div>
<% end %>
